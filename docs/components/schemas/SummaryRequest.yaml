SummaryRequest:
  type: object
  properties:
    source:
      description: The text to be summarized
      type: string
    model:
      description: The model used to generate the summary
      type: string
      enum:
        - t5-large
    params:
      $ref: ./Params.yaml
  required:
    - source